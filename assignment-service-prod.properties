# ================== ASSIGNMENT SERVICE PROD CONFIGURATION ==================

# Database Configuration
spring.datasource.url=jdbc:mysql://prod-db-server:3306/citizenfix_assignment_db
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Hibernate
spring.jpa.hibernate.ddl-auto=validate

# Logging
logging.level.com.citizenfix=INFO
logging.level.com.citizenfix.assignment=INFO
